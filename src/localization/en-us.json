{
  "commands": "#extends commands; Extends on the root locale is relative to a folder with the same name as the locale.",
  "common": "#extends ~/en-us/common; If you want to reference a file from the localization folder you can use the '~/' path."
}